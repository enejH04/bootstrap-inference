[project]
name = "bootstrap-diagnostics"
version = "0.1.0"
description = "Double bootstrap with diagnostic tests"
readme = "README.md"
authors = [
    { name = "enejH04", email = "hudobreznik.enej.04@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.2",
]

[build-system]
requires = ["uv_build>=0.10.2,<0.11.0"]
build-backend = "uv_build"
